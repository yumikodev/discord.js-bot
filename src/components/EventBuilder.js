class EventBuilder {
  /**
   * @param {import("../types/EventBuilderOptions").EventBuilderOptions} options
   */
  constructor(options) {
    this.data = options;
  }
}

module.exports = EventBuilder;
